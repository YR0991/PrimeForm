# Strava OAuth
# Lokaal: kopieer naar .env en vul in.
# Render: zet deze in Dashboard → Environment (geen .env bestand op Render).
STRAVA_CLIENT_ID=172387
STRAVA_CLIENT_SECRET=
STRAVA_REDIRECT_URI=http://localhost:3000/auth/strava/callback

# Strava Webhooks (near real-time activity ingest)
# STRAVA_VERIFY_TOKEN: token you choose; must match the value used when creating the subscription.
# STRAVA_WEBHOOK_CALLBACK_URL: full URL Strava will call (GET for verify, POST for events). Example: https://your-backend.onrender.com/webhooks/strava
STRAVA_VERIFY_TOKEN=
STRAVA_WEBHOOK_CALLBACK_URL=https://your-backend.onrender.com/webhooks/strava

# Waar de gebruiker na Strava-koppeling naartoe wordt gestuurd
FRONTEND_APP_URL=http://localhost:9000

# --- SMTP (Nodemailer) — required for intake notification emails ---
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM= (optional; defaults to SMTP_USER)

# --- Productie (Render): gebruik deze waarden in Environment ---
# STRAVA_REDIRECT_URI=https://primeform-backend.onrender.com/auth/strava/callback
# FRONTEND_APP_URL=https://prime-form-frontend2701.vercel.app
